# Read data

Import the data.
Data files are locally stored.

```{r}
D1 <- importData("data/raw/Sample stats formatting - Gillingham.xlsx")
```

Check the data.

```{r}
tableChr(D1)
summary(D1$value)
```
